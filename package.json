{
  "name": "weather-radar-display",
  "version": "1.0.0",
  "description": "Full-stack weather radar display using MRMS data",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "deploy:build": "npm run build",
    "deploy:start": "npm start",
    "deploy:full": "npm run install-all && npm run deploy:build && npm run deploy:start"
  },
  "keywords": ["weather", "radar", "MRMS", "React", "Node.js"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
